
class Trainer():
    def __init__(self, model, dataset, optimizer, kfold, save_dir, best_dir, config):
        pass

    # for fold, (train_idx, test_idx) in enumerate(kfold.split(dataset.loaded_chromas)):
    #     print(fold)
    #     # print(train_idx)
    #     # print(test_idx)